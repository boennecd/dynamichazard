## -- compiling for OpenMP and c++11 n more
PKG_CXXFLAGS = -std=c++11 ## -fopenmp
## -- linking (do not ask if all of these are needed. I saw them in sourcecpp call with verbose = T)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) ## -lm -lquadmath  -fopenmp
